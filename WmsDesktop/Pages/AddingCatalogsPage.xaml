<Page x:Class="WmsDesktop.Pages.AddingCatalogsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:WmsDesktop.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Loaded="Page_Loaded"
      Title="AddingCatalogsPage"
      >

    <Grid Background="White">
        <StackPanel Orientation="Horizontal">
            <StackPanel
            Width="200"
            >
                <StackPanel 
                Margin="5"
                Orientation="Horizontal">
                    <TextBlock Margin="5">Имя</TextBlock>
                    <TextBox x:Name="tbName" Width="150" Text="{Binding TbName, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                </StackPanel>
                <StackPanel  Margin="5"
                Orientation="Horizontal">
                    <TextBlock Margin="5"> ШК</TextBlock>
                    <TextBox x:Name="tbBarcode" Width="150" Text="{Binding TbBarcode, UpdateSourceTrigger=PropertyChanged}"></TextBox>
                </StackPanel>
                <Button x:Name="btnCatalogBorkAdd" 
                    Height="30" 
                    Width="120"
                    Click="btnCatalogBorkAdd_Click">Добавить</Button>
                <TextBlock x:Name="tbError" Text="{Binding TblockError, UpdateSourceTrigger=PropertyChanged}"></TextBlock>
            </StackPanel>
            <StackPanel>

                <StackPanel Orientation="Horizontal">
                    <TextBox x:Name="textBoxSearhc" Text="{Binding TbText, UpdateSourceTrigger=PropertyChanged}" TextChanged="textBoxSearhc_TextChanged" 
                    Padding="5"
                    Margin="5"
                    Width="200"></TextBox>

                </StackPanel>
                <ListBox
                    ItemsSource="{Binding ItemsList}"
                    Width="600" 
                    Height="380"
                    ScrollViewer.VerticalScrollBarVisibility="Auto" 
                                        x:Name="listItems"
                                        SelectionChanged="listItems_SelectionChanged">

                </ListBox>
            </StackPanel>
        </StackPanel>
    </Grid>
</Page>
